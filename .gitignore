# Dependencies
node_modules/
**/node_modules/

# Environment
.env
.env.local
.env.*.local

# Build output
client/dist/
dist/
build/

# Runtime
uploads/
*.log

# Test/screenshot artefacts (top-level only — keep docs/screenshots/)
/screenshots/
remotion-bg/out/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Optional caches
.cache/
.parcel-cache/
.next/
.turbo/

# Dev-tool output (overlays, probe reports, screenshots).
# Anchored: a bare `dev/` also matched scripts/dev/, so none of the dev tooling
# the README, CLAUDE.md and package.json point at was ever committed.
/dev/


# Sprint scratch tools — screenshots and one-off probes, not part of the repo.
scripts/dev/_*.ts

# Candidate demo faces awaiting vetting — images only, README is committed.
eval/candidates/*
!eval/candidates/README.md

# Source product photos for the demo shop check. Third-party images, often with a
# shop's label on them; only the cropped copy in client/public/demo-products/ is
# published. Pass --image=<path> to scripts/precomputeDemoCheck.ts on a fresh clone.
design/demo-products/
